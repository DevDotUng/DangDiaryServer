<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dangdiary.api.dao.MyDiaryDAO">
  <select id = "getMyDiaryDogDTO" parameterType = "int" resultType = "com.dangdiary.api.dto.myDiary.MyDiaryDogDTO">
    SELECT dog_name dogName, profile_image profileImage, birth startDate 
    FROM dogs 
    WHERE user_id=#{userId};
  </select>
  <select id = "getMyDiaryNumbersDTO" parameterType = "int" resultType = "com.dangdiary.api.dto.myDiary.MyDiaryNumbersDTO">
    SELECT sum(number_of_like) numberOfLike 
    From diarys 
    WHERE user_id=#{userId};
  </select>
  <select id = "getMyDiaryMonthDTO" parameterType = "int" resultType = "com.dangdiary.api.dto.myDiary.MyDiaryNumbersDTO">
    SELECT sum(number_of_like) numberOfLike 
    From diarys 
    WHERE user_id=#{userId};
  </select>
</mapper>